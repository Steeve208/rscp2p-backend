# Let's Encrypt STAGING + DigitalOcean DNS-01 — wildcard (*.example.com).
# Requires Secret digitalocean-dns-token in namespace cert-manager.
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-staging-dns-digitalocean
  labels:
    app.kubernetes.io/name: rsc-gateway
spec:
  acme:
    server: https://acme-staging-v02.api.letsencrypt.org/directory
    email: LETSENCRYPT_EMAIL@example.com
    privateKeySecretRef:
      name: letsencrypt-staging-dns-digitalocean-account-key
    solvers:
      - dns01:
          digitalocean:
            tokenSecretRef:
              name: digitalocean-dns-token
              key: access-token
