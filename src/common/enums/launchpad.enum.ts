export enum ContributionStatus {
  ACTIVE = 'active',
  FULLY_VESTED = 'fully-vested',
}

export enum SubmissionStatus {
  PENDING = 'pending',
  APPROVED = 'approved',
  REJECTED = 'rejected',
}

export enum SentimentVote {
  BULLISH = 'bullish',
  BEARISH = 'bearish',
}
